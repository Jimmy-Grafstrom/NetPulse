spring:
  application:
    name: NetPulse

netpulse:
  monitor:
    target-host: google.com
    interval-ms: 5000
    thresholds:
      warning-ms: 100
      critical-ms: 500
  logging:
    csv-path: logs/network-metrics.csv